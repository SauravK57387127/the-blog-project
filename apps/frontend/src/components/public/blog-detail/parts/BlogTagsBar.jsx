export default function BlogTagsBar() {
    return <div>This contains all the tags a particular blog has.</div>;
}

// #### 4. **Blog Tags Strip**

// * `BlogTagsBar.jsx`
// * Tags shown in a strip or badge list
// * Optional: clicking tag → navigate to homepage with `?tag=...`

// ### ✅ 4. `BlogTagsBar.jsx`

// * Uses blog data (tags part of blog object)
// * No separate endpoint
