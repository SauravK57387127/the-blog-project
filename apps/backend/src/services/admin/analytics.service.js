// Aggregates Postgres analytics, optional Redis cache
export default {
  // Global KPIs for dashboard
  overview: async (range) => { /* TODO */ },

  // Per‑blog deep dive
  blogStats: async (blogId, range) => { /* TODO */ },
};
